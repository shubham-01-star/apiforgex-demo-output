book-library-api

Generated by ApiforgeX 🚀

This is a production-ready Backend API built with Express, Prisma, and TypeScript.

🛠️ Tech Stack

Framework: Express.js

Database: postgresql

ORM: Prisma

Language: TypeScript

🚀 Getting Started

1. Install Dependencies

npm install


2. Configure Environment

Open the .env file.

Update the DATABASE_URL with your real database credentials.

Example for PostgreSQL:
DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"

3. Sync Database

Push the schema to your database to create the tables.

npx prisma db push


4. Start the Server

npm run dev


The server will start at http://localhost:3000.

📡 API Endpoints

Health Check

GET / - Check if API is running.


Book

GET    /api/books      - Get all Books

GET    /api/books/:id  - Get Book by ID

POST   /api/books      - Create a new Book

DELETE /api/books/:id  - Delete Book

BookAuthor

GET    /api/bookauthors      - Get all BookAuthors

GET    /api/bookauthors/:id  - Get BookAuthor by ID

POST   /api/bookauthors      - Create a new BookAuthor

DELETE /api/bookauthors/:id  - Delete BookAuthor

Genre

GET    /api/genres      - Get all Genres

GET    /api/genres/:id  - Get Genre by ID

POST   /api/genres      - Create a new Genre

DELETE /api/genres/:id  - Delete Genre

ISBN

GET    /api/isbns      - Get all ISBNs

GET    /api/isbns/:id  - Get ISBN by ID

POST   /api/isbns      - Create a new ISBN

DELETE /api/isbns/:id  - Delete ISBN

PublicationYear

GET    /api/publicationyears      - Get all PublicationYears

GET    /api/publicationyears/:id  - Get PublicationYear by ID

POST   /api/publicationyears      - Create a new PublicationYear

DELETE /api/publicationyears/:id  - Delete PublicationYear

🧪 Testing

You can test these endpoints using Postman or curl.

# Example: Create a new Book
curl -X POST http://localhost:3000/api/books \
  -H "Content-Type: application/json" \
  -d '{"someField": "value"}'
Replace "someField" and "value" with actual fields for the Book entity.
📦 Project Structure
/src
  /controllers   - Route handlers for API endpoints
  /models        - Prisma schema and database models
  /routes        - Express route definitions
  /utils         - Utility functions
  server.ts      - Entry point of the application       

